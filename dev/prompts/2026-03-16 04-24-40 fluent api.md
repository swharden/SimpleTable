> I want to modify the StringTable class to support a fuent API that allows chaining of operations. Can you refactor the methods in that class that return void to return StringTable (this) instead? Try not to modify any other functinality inside the methods. Add tests to the FluentApiTests nunit test class demonstrating how to use the fluent API at a high level.

Let me read the full `StringTable.cs` file to see the exact code before making changes.
Now I have a complete picture. I'll update all `void`-returning public methods to return `StringTable` (`this`). 

Let me apply all changes in one batch:
All 27 methods updated. 

Now let me add the fluent API tests:
All 7 tests pass. 

Here's a summary of what was changed:

**`StringTable.cs`** — 27 public `void` methods were updated to return `StringTable` (via `return this;`):
- **Column operations**: `AddColumn` (4 overloads), `AddColumns` (3 overloads), `DeleteColumn` (3 overloads)
- **Row operations**: `AddRow` (3 overloads), `AddRows` (2 overloads), `DeleteRow` (2 overloads)
- **Table operations**: `ClearRows`, `Clear`, `Expand`, `Rotate90`
- **Data access**: `SetValue` (3 overloads) — converted from expression bodies to block bodies
- **Column names**: `SetColumnName`, `SetColumnNames`, `SetColumnNamesFromFirstRow`

**`FluentApiTests.cs`** — 7 tests added demonstrating:
- Chaining `AddColumn` calls
- Chaining `AddRow` calls
- Chaining `SetValue` calls
- Setting column names in a chain
- Deleting columns/rows in a chain
- `Expand` + `ClearRows` in a chain
- A complex end-to-end chain combining multiple operations